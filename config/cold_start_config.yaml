seed: 42

defaults:
  - _self_       # 2. Let the rest of this YAML file override the schema defaults
  - clarification_model/base: qwen_3_vl_32b_lora_bfloat16
  - clarification_model/lora: qwen_3_vl_32b_lora_bfloat16

paths:
  data:
    trees_subpath: v4
  checkpoints:
    loras_subpath: v4/loras
    merged_models_subpath: v4/merged_models

runtime_meta:
  git_commit: null
  git_strict: true

wandb:
  project: "clarification-cold-start"
  name: null
